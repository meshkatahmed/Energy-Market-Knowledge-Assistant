services:
  backend:
    build:
      context: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
      - HFHUB_API_TOKEN=${HFHUB_API_TOKEN}
    develop:
      watch:
        - path: ./pyproject.toml
          action: rebuild
        - path: ./poetry.lock
          action: rebuild
        - path: .
          target: /app
          action: sync
    restart: always

  frontend:
    build:
      context: ./frontend
    ports:
      - "80:80"
    env_file:
      - ./frontend/.env
    environment:
      - VITE_BACKEND_URL=${VITE_BACKEND_URL}
    develop:
      watch:
        - path: ./frontend/dist
          target: /usr/share/nginx/html
          action: sync
    depends_on:
      - backend
    restart: always


# volumes:
    #   - .:/app
    #   - ./app/data/chroma_db:/app/app/data/chroma_db
    #   - ./app/data/raw_data:/app/app/data/raw_data

# volumes:
    #   - ./frontend:/app
    #   - /app/node_modules

# - CHOKIDAR_USEPOLLING=true
# command: npm run dev -- --host